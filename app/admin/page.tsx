export default function Admin(){return <div><h1 className="text-3xl font-bold">Admin & Google Sheets Setup</h1><div className="card p-6 mt-5 space-y-4"><p>This starter runs immediately with sample data. For live Google Form responses, use Google Apps Script to POST each new response to:</p><code className="block bg-black p-3 rounded break-all">/api/google-sheets</code><p className="muted">Send the shared secret in the <code>x-webhook-secret</code> header. Configure the secret in <code>GOOGLE_SHEETS_WEBHOOK_SECRET</code>.</p><p>When Supabase is configured, replace the sample repository with live queries and the endpoint will insert/update driver availability.</p></div><h2 className="text-xl font-bold mt-7">Expected payload</h2><pre className="card p-5 overflow-auto mt-3">{`{ "name":"Driver Name", "email":"driver@example.com", "club":"Club", "experience":"Advanced", "classes":["Touring Car"], "rounds":["r1","r2"], "status":"looking", "notes":"..." }`}</pre></div>}