import Link from "next/link"; import {rounds,drivers} from "@/lib/sample";
export default function Home(){const open=drivers.filter(d=>d.status!=="affiliated").length;return <div className="space-y-8"><section className="card p-8"><p className="text-red-400 font-bold">RC ENDURANCE RACING</p><h1 className="text-4xl font-bold mt-2">Find the drivers your team needs.</h1><p className="muted mt-3 max-w-2xl">A round-by-round driver pool for endurance teams. Drivers declare availability; team managers filter unaffiliated drivers and build a shortlist.</p><div className="mt-5 flex gap-3"><Link className="btn" href="/drivers">Browse available drivers</Link><Link className="px-4 py-2 border border-slate-600 rounded-lg" href="/admin">Configure data</Link></div></section><section className="grid md:grid-cols-3 gap-4"><div className="card p-5"><b>{rounds.length}</b><p className="muted">sample championship rounds</p></div><div className="card p-5"><b>{open}</b><p className="muted">drivers currently open to approaches</p></div><div className="card p-5"><b>Google Sheets ready</b><p className="muted">Webhook endpoint included</p></div></section><section><h2 className="text-2xl font-bold mb-4">Championship calendar</h2><div className="grid md:grid-cols-3 gap-4">{rounds.map(r=><div key={r.id} className="card p-5"><b>{r.name}</b><p className="muted mt-2">{r.date}</p><Link className="text-red-300 text-sm" href={`/drivers?round=${r.id}`}>Find drivers →</Link></div>)}</div></section></div>}